Write a function called `shortest()` that takes an array of names as an argument and returns the shortest name.
	
Examples: 	

`shortest(["kevin", "maurya", "rebecca", "eric"])`

should return `eric`
